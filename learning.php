<?php
/**
 * Created by PhpStorm.
 * User: Edward
 * Date: 10/02/2019
 * Time: 01:50
 */

/* Check if database was affected by an UPDATE
 * if (mysqli_affected_rows($connection) > 0) {
        //did affect
        echo "Query did update a row";
        $connection->close();
    } else {
        //did not affect
        echo "Query did not update a row";
        $connection->close();
    }
 */

/* Get IP of user
 * $ip = $_SERVER['REMOTE_ADDR'];
 */